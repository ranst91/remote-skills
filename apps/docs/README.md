# Remote Skills documentation

The self-hostable Fumadocs application for Remote Skills.

```bash
pnpm --filter @remote-skills/docs dev
pnpm --filter @remote-skills/docs check
```

`pnpm build` produces the production Next.js application, served by `pnpm start`. The docs do not require a central Remote Skills service.
