{
  "name": "@remote-skills/langchain",
  "version": "0.0.1",
  "description": "Verified remote skills through native DeepAgents and LangChain middleware.",
  "type": "module",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=24.0.0"
  },
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "peerDependencies": {
    "@remote-skills/client": "^0.0.1",
    "deepagents": "~1.13.4",
    "langchain": "~1.5.11"
  },
  "devDependencies": {
    "@langchain/core": "1.2.10",
    "@langchain/langgraph": "1.4.14",
    "@remote-skills/client": "workspace:*",
    "@types/node": "catalog:",
    "deepagents": "1.13.4",
    "langchain": "1.5.11",
    "langsmith": "0.9.0",
    "typescript": "catalog:",
    "zod": "4.3.6"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test tests/*.test.ts",
    "package:check": "node scripts/check-package.ts",
    "check": "pnpm typecheck && pnpm test"
  }
}
