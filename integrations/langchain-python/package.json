{
  "name": "@remote-skills/langchain-python-workspace",
  "version": "0.0.0",
  "private": true,
  "license": "Apache-2.0",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "typecheck": "node ../../scripts/run-uv.ts run --locked --package remote-skills-langchain python -m compileall -q src tests",
    "test": "node ../../scripts/run-uv.ts run --locked --package remote-skills-langchain python -m unittest discover -s tests",
    "package:check": "node ../../scripts/run-uv.ts run --locked --package remote-skills-langchain python tests/package_smoke.py",
    "check": "pnpm typecheck && pnpm test"
  }
}
