[project]
name = "remote-skills-langchain-example"
version = "0.0.0"
requires-python = ">=3.11,<4.0"
dependencies = ["remote-skills-langchain==0.0.1", "langchain-openai==1.6.2"]

[tool.uv]
package = false

[tool.uv.sources]
remote-skills-langchain = { workspace = true }
