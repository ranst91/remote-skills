{
  "name": "@remote-skills/example-basic-python-agent",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "postinstall": "node ../../../scripts/examples/sync-python.ts",
    "dev": "uv run --locked python server.py",
    "check": "uv run --locked python -c \"import agent, server; import py_compile; py_compile.compile('agent.py', doraise=True); py_compile.compile('server.py', doraise=True)\""
  }
}
