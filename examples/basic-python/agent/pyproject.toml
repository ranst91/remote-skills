[project]
name = "remote-skills-basic-python-agent"
version = "0.0.0"
requires-python = ">=3.11"
dependencies = [
    "openai==3.10.0",
    "remote-skills",
    "starlette==1.6.0",
    "uvicorn==0.52.4",
]

[tool.uv]
package = false

[tool.uv.sources]
remote-skills = { path = "../../../packages/sdk-python", editable = true }
