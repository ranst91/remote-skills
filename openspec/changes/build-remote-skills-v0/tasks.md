## 1. Approval gates

- [x] 1.1 **Approval owner — no code:** Review and explicitly approve the complete OpenSpec proposal, capability specs, design, and this dependency map; verify `openspec validate build-remote-skills-v0 --strict` passes. **Blocks every task below.**
- [x] 1.2 **README owner — no code:** Draft the complete minimal replacement README from the approved OpenSpec, paste it into the user conversation, revise it until explicitly approved, and only then replace `README.md`; verify the approved text and committed file are byte-equivalent. **Depends on 1.1 and blocks every implementation task.**

## 2. Repository foundation

- [x] 2.1 **Session `repo-foundation`; owns root manifests/config only:** Create the pnpm workspace, Turborepo graph, Node.js 24 baseline, uv Python workspace, Apache-2.0 license, formatting/lint/typecheck/test root commands, and package/app/example directories from `design.md`; verify clean installs and empty-package checks on a fresh clone. **Depends on 1.2.**
- [x] 2.2 **Session `repo-foundation`; owns CI bootstrap only:** Add the primary Linux CI shell, a focused Windows installed-product shell, dependency caching, deterministic environment variables, and root `check` orchestration without publication credentials; verify every declared project is owned by a real gate rather than silently skipped. **Depends on 2.1.**
- [x] 2.3 **Session `repo-foundation`; owns schema tooling and generated-schema checks:** Add the strict `remote-skills.json` JSON Schema, schema publication path, unknown-field rejection tests, and a drift check tying runtime validators to the checked-in schema; verify valid defaults and invalid typo fixtures in both direct CLI tests and schema validation. **Depends on 2.1.**
- [x] 2.4 **Session `agent-accessibility-foundation`; owns root `AGENTS.md` only:** Add one concise, tool-neutral contributor-agent entry point covering project purpose, canonical architecture/ownership references, supported setup and verification commands, documentation locations, and release-safety boundaries without duplicating detailed docs or adding vendor-specific instruction files; verify every referenced path and command exists. **Depends on 2.1 and must land before the next implementation wave.**

## 3. Shared protocol contract

- [x] 3.1 **Session `protocol-contracts`; owns `tests/protocol/**` and shared error definitions:** Define stable error codes, normalized result shapes, catalog fixtures, request transcripts, redaction expectations, and static expected results; capture RED in both SDK test adapters before either implementation can pass. **Depends on 2.1 and 2.3.**
- [x] 3.2 **Session `protocol-contracts`; owns archive/path/limit fixtures:** Add exact valid and adversarial `skill-md`, tar-gzip, ZIP, traversal, duplicate, case-collision, link, special-file, invalid-UTF-8, decompression, count, and size fixtures without generating expectations from code under test; verify fixture integrity with independent SHA-256 manifests. **Depends on 3.1.**
- [x] 3.3 **Session `protocol-contracts`; owns cache/network fixtures:** Add versioned cache-layout, partial-writer, crashed-lease, cross-process, HTTP validator, redirect, DNS/IP policy, retry, offline, removal, and credential-forwarding fixtures; verify the fixture server records exact requests and never stores secret values in snapshots. **Depends on 3.1.**
- [x] 3.4 **Session `protocol-contracts`; owns deterministic publisher goldens:** Add canonical source trees plus independently reviewed exact index, Markdown, tar-gzip, ZIP, normalized metadata, and digest expectations; verify fixture bytes are stable when unpacked and repacked by an independent inspection script. **Depends on 3.2.**
- [x] 3.5 **Session `scope-version-contracts`; owns only supplemental `tests/protocol/**` fixtures/expectations and shared error definitions:** Add exact `Remote-Skills-Scope` request/confirmation, `401`/`403`, `no-store`, authenticated-unconfirmed memory-only, cache-isolation, artifact-authorization, versioned/unversioned `x-remote-skills`, strict SemVer range, prerelease, immutable-mapping, removal/stale, and `version_unavailable` contracts. Include secret canaries, static expected results, malformed and 100-release-bound fixtures, and RED adapters for both SDKs without regenerating existing expected results. **Depends on 3.1 and 3.3; blocks 4.2, 5.6, 6.6, and every activation/session task.**

## 4. Publisher CLI

- [x] 4.1 **Session `publisher-validation`; owns `packages/core/src/authoring/**` and CLI validate/config modules:** Write RED tests, then implement Agent Skills frontmatter validation, directory/name rules, strict declarative config, safe default exclusions, `.skillignore`, filesystem boundary checks, symlink/hard-link rejection, local-reference validation, warning/error separation, and `--strict`; verify package-local tests and relevant protocol fixtures pass. **Depends on 3.1 and 3.2.**
- [x] 4.2 **Session `publisher-build`; owns `packages/core/src/build/**` and CLI build modules:** Write RED deterministic fixtures, then implement `skill-md` selection, forced archives, normalized tar-gzip/ZIP encoders, content-addressed filenames, relative v0.2.0 index generation, optional strict `metadata.version`, deterministic bounded `x-remote-skills` history, verified prior-output carry-forward, explicit pruning, immutable skill/version mapping, limit enforcement, and atomic output replacement. Preserve ordinary top-level current fields, error rather than exceed 100 releases, and verify byte-exact goldens on the current OS and two consecutive builds. **Depends on 4.1, 3.4, and 3.5.**
- [x] 4.3 **Session `publisher-dev`; owns CLI dev/server modules:** Write RED local-origin and interrupted-rebuild tests, then implement loopback `127.0.0.1:8787` serving, host/port options, explicit `--unsafe-host`, source watching, atomic completed-output switching, graceful shutdown, and no push protocol; verify a real local HTTP consumer observes only complete catalog generations. **Depends on 4.2.**
- [x] 4.4 **Session `publisher-verify`; owns CLI verify/network modules:** Write RED private-origin and tampering tests, then implement full-origin verification, `--header`, `--header-env`, requested scope, canonical-scope confirmation, credential-safe diagnostics, version-extension validation, network/redirect policy, bounded timeout/retries, and every-entry/release failure aggregation; verify local scoped origins, `401`/`403`, artifact enforcement, CDN header isolation, digest mismatch, timeout, and private-address redirect fixtures. **Depends on 4.2, 3.3, and 3.5.**
- [x] 4.5 **Session `publisher-cli-gate`; owns CLI package manifest/entrypoint/tests only:** Wire `@remote-skills/cli` with binary `remote-skills`, help/version output, exit codes, and package contents; create a local package tarball, install it into a clean temporary project with the locked package manager under Node.js 24, and invoke that installed `remote-skills` binary directly. Direct invocation from the local tarball installation remains mandatory acceptance; after that local installation, `npx --no-install remote-skills` may be exercised only as a convenience path. Do not use package-manager download-on-demand, a registry, ordinary `npx` registry fallback, or an unpackaged workspace binary. Verify `validate`, `build`, `dev`, and `verify` invoke only their intended boundaries and that no `bridge`, `deploy`, or `publish` command exists. **Depends on 4.3 and 4.4.**

## 5. TypeScript consumer SDK

- [x] 5.1 **Session `typescript-client`; owns `packages/sdk-typescript/src/catalog/**` and origin configuration:** Write RED catalog fixtures, then implement explicit aliases, compact aggregate and single-origin discovery, v0.2.0-only schema handling, unknown-field tolerance, standard HTTP caching, partial failure, strict aggregate mode, URL resolution, headers, timeout/retry, and network policy; verify exact request transcripts and stable error codes. **Depends on 3.1 and 3.3.**
- [x] 5.2 **Session `typescript-cache`; owns `packages/sdk-typescript/src/cache/**`:** Write RED atomicity and lease tests, then implement OS-default `cache-v1`, memory/custom caches, verified no-clobber publication, catalog metadata, cross-process leases, crash cleanup, bounded LRU eviction, and credential exclusion; verify multi-process readers never observe partial state. **Depends on 3.3.**
- [x] 5.6 **Session `typescript-scope-version`; owns supplemental TypeScript origin/catalog configuration, catalog cache identity, SemVer resolver, stable errors, and their tests only:** Add public `scope`, exact `Remote-Skills-Scope` exchange, canonical-scope validation, origin-plus-confirmed-scope persistent identity, authenticated-unconfirmed memory-only behavior, `no-store`, `authentication_failed`, `authorization_denied`, bounded `x-remote-skills` parsing, and deterministic highest-compatible SemVer selection. Keep credentials out of keys/metadata/diagnostics and pass every supplemental shared fixture before activation work. **Depends on 5.1, 5.2, and 3.5.**
- [x] 5.3 **Session `typescript-activation`; owns `packages/sdk-typescript/src/activation/**` and activated resource types:** Write RED artifact fixtures, then implement digest-first activation, `skill-md`, bounded tar-gzip/ZIP extraction, normalized safe paths, standard frontmatter parsing, immutable authorized scope/version/descriptor/digest session pins, `list`, UTF-8 `read`, `readBytes`, and no execution; verify all archive/path/limit and supplemental scope/version fixtures. **Depends on 5.1, 5.2, 5.6, 3.2, and 3.5.**
- [x] 5.4 **Session `typescript-session`; owns TypeScript client/session facade and lifecycle tests:** Write RED update/offline/removal tests, then expose `createRemoteSkills`, aggregate `catalog`, origin-bound `session`, SemVer-range activation, future-session `refresh`, stale-age metadata, scope/release removal behavior, `close`, and async disposal; verify D1/version/scope remains pinned while compatible updates or removal affect only later online sessions and stale sessions are explicit. **Depends on 5.3.**
- [x] 5.5 **Session `typescript-package-gate`; owns TypeScript manifest/exports/browser rejection/package smoke tests:** Expose a server-runtime-only `@remote-skills/client` surface with declarations, ESM/CJS strategy chosen consistently, no browser bundle, and clean-install examples; verify local packed installation and import on Node.js 24 with no workspace or registry resolution. **Depends on 5.4.**

## 6. Python consumer SDK

- [x] 6.1 **Session `python-client`; owns `packages/sdk-python/src/remote_skills/catalog*` and origin configuration:** Write RED shared catalog fixtures, then implement the async explicit-origin catalog/network behavior with exact TypeScript-equivalent requests, v0.2.0 handling, caching validators, partial failures, headers, timeout/retry, and network policy; verify stable error-code parity. **Depends on 3.1 and 3.3.**
- [x] 6.2 **Session `python-cache`; owns Python cache modules:** Write RED shared-layout and process-race tests, then implement OS-default `cache-v1`, memory/custom caches, atomic immutable publication, leases, crash cleanup, bounded LRU, and credential exclusion; verify Python reuses TypeScript-written fixtures and vice versa. **Depends on 3.3.**
- [x] 6.6 **Session `python-scope-version`; owns supplemental Python origin/catalog configuration, catalog cache identity, SemVer resolver, stable errors, and their tests only:** Add public `scope`, exact `Remote-Skills-Scope` exchange, canonical-scope validation, origin-plus-confirmed-scope persistent identity, authenticated-unconfirmed memory-only behavior, `no-store`, `authentication_failed`, `authorization_denied`, bounded `x-remote-skills` parsing, and deterministic highest-compatible SemVer selection. Keep credentials out of keys/metadata/diagnostics and pass the same supplemental fixtures as TypeScript before activation work. **Depends on 6.1, 6.2, and 3.5.**
- [x] 6.3 **Session `python-activation`; owns Python activation/archive/resource modules:** Write RED shared artifact fixtures, then implement digest-first activation, `skill-md`, safe bounded tar-gzip/ZIP extraction, standard parsing, immutable authorized scope/version/descriptor/digest pins, async `list`/`read`/`read_bytes`, and no execution; verify exact normalized parity across every archive/path/limit and supplemental scope/version fixture. **Depends on 6.1, 6.2, 6.6, 3.2, and 3.5.**
- [x] 6.4 **Session `python-session`; owns Python facade/lifecycle tests:** Write RED update/offline/removal tests, then expose `RemoteSkills`, aggregate catalog, async origin session/context manager, SemVer-range activation, future-session refresh, stale metadata, scope/release removal behavior, and explicit async close; verify behavior matches TypeScript golden results for compatible updates, online authority, and explicit stale sessions. **Depends on 6.3.**
- [x] 6.5 **Session `python-package-gate`; owns `pyproject.toml`, Python exports, build/smoke tests:** Produce the `remote-skills` wheel and sdist for Python 3.11+, import as `remote_skills`, exclude workspace-only sources, and verify clean uv installation and async quickstart execution. **Depends on 6.4.**

## 7. Cross-language and security convergence

- [x] 7.1 **Session `protocol-parity`; owns only shared fixture adapters/expected-result corrections:** Run every protocol fixture through publisher, TypeScript, and Python; resolve semantic differences without weakening fixtures and verify exact outputs, stable errors, request counts, and cache effects are identical. **Depends on 4.5, 5.5, and 6.5.**
- [x] 7.2 **Session `cache-race-gate`; owns cross-process integration tests:** Exercise simultaneous TypeScript/Python downloads, losing writers, partial crashes, lease expiry, active-pin eviction, cache corruption, and stale temporary cleanup against one real directory; verify no partial/corrupt observation and no active artifact eviction. **Depends on 7.1.**
- [x] 7.3 **Session `network-security-gate`; owns adversarial integration tests:** Exercise same-host and cross-host headers, redirect chains, DNS/IP changes, loopback development exceptions, userinfo/query sanitization, request limits, retry exhaustion, and secret canaries; verify secrets appear only on their configured host and never in cache, errors, debug events, or snapshots. **Depends on 4.4, 5.5, and 6.5.**
- [x] 7.4 **Session `determinism-gate`; owns platform-independent golden checks:** Run publisher goldens in the primary Linux CI environment, compare raw artifact/index hashes with the checked expectations, then consume the exact output in both SDKs; verify byte identity and interoperability with cache disabled and enabled. **Depends on 7.1 and CI task 2.2.**
- [x] 7.5 **Session `scope-version-parity`; owns only supplemental cross-language/network/cache integration tests and evidence:** Prove scope names cannot self-grant, exact `401`/`403` mapping, artifact-side enforcement, no cross-scope or credential leakage, no-store/unconfirmed persistence rules, identical SemVer and prerelease resolution, immutable version mapping, online release removal, bounded stale use, and scope/version session pins across TypeScript and Python. Verify a standard v0.2 reader can ignore `x-remote-skills`; do not weaken prior fixtures. **Depends on 4.4, 4.5, 5.5, 6.5, and 7.1; blocks final acceptance.**

## 8. Documentation and examples

- [x] 8.1 **Session `docs-site`; owns `apps/docs/**` only:** Build the self-hostable Fumadocs site with conceptual overview, exact archive-to-origin hosting, Git-backed CI/Pages/custom-domain hosting and project-subpath caveat, CLI, TypeScript, Python, disk/cache/update/offline, scoped authentication/authorization, SemVer policy/history/removal, trust/security, configuration, API reference, and release pages. Include the local Docker/Git-clone decision path, persistent-volume guidance, ephemeral-container cost, and full-download versus context-lazy distinction; verify links, navigation, production build, and boundary-language tests. **Depends on approved README task 1.2 and stable APIs 4.5, 5.5, 6.5.**
- [x] 8.2 **Session `examples`; owns `examples/**` only:** Implement runnable publisher, TypeScript consumer, Python consumer, generic static-host, authenticated-scope, version-range, and Git repository → CI → Pages/custom-domain examples using only public package surfaces. Verify a complete generated `dist/` tree through `verify`, origin-root well-known resolution, `401`/`403` plus artifact enforcement, and both consumers against the same local origin. **Depends on 4.5, 5.5, and 6.5.**
- [x] 8.3 **Session `docs-contract`; owns snippet extraction/tests and may edit docs examples but not APIs:** Compile or execute every README/docs/config snippet; verify the minimal README and full docs answer what exact built bytes to host, do not claim a hosted service or adapters, and accurately distinguish byte integrity from content trust, disk from RAM, context-lazy reads from full archive download, scope requests from authorization grants, and ranges from provider retention. **Depends on 8.1, 8.2, 8.4, and 8.5.**
- [x] 8.4 **Session `agent-accessibility-docs`; owns docs-site `llms.txt`, Markdown-route configuration, and related navigation metadata only:** Publish a concise standards-shaped `/llms.txt` that points to clean Markdown versions of the canonical quickstarts, archive-to-origin and Git-backed hosting, CLI, TypeScript, Python, scoped authentication/authorization, SemVer policy, cache/offline, trust/security, and API docs; derive or validate it against canonical docs navigation, keep essential guidance accessible without client-side interaction, and verify the production docs build contains the index and resolves every listed route. Do not add live-model tests, `llms-full.txt`, or vendor-specific documentation copies. **Depends on 8.1.**
- [x] 8.5 **Session `readme-hosting-path`; owns `README.md` only:** Add a concise “I bundled it; what do I host?” path covering complete `dist/` upload/mount, fixed well-known catalog, relative artifacts, exact bytes/digests, MIME types, root `SKILL.md`, verification, and SDK origin configuration. Point to the Git-backed/static-host guide and state when local image copy/clone is preferable; preserve the approved minimal README voice and do not introduce `npx`-on-demand, deploy, publish, or hosted-service claims. **Depends on stable publisher/API tasks 4.5, 5.5, and 6.5.**

## 9. Local packaging and publication-readiness

- [x] 9.1 **Session `package-artifacts`; owns package manifests and local package-check scripts:** Add coordinated version metadata starting at `0.0.1`, independent package boundaries, Apache-2.0 package contents, local npm pack and uv build checks, SBOM/provenance inputs, and clean offline installation smoke tests; verify no workspace-only dependency or unintended file leaks into artifacts. **Depends on 4.5, 5.5, and 6.5.**
- [x] 9.2 **Session `publication-readiness`; owns local artifact-inspection scripts and CI package-build gates only:** Build, inspect, hash, and clean-install the npm tarballs and Python wheel/source distribution in the primary Linux package lane without registry access, and clean-install and run each public distribution in the focused Windows lane. Record publication-readiness evidence without registry configuration, OIDC permissions, credentials, tags, uploads, package-name claims, or a release/publication workflow. **Depends on 2.2 and 9.1.**
- [x] 9.3 **Session `no-publication-gate`; owns final boundary evidence only:** Inspect commands, scripts, workflows, docs, and acceptance output to verify no npm/PyPI publish/upload command, package-name reservation, registry/Trusted Publishing/OIDC configuration, publication credential, tag-triggered publication job, or claim of completed publication exists. **Depends on 9.2 and blocks final acceptance.**

## 10. Strict TypeScript architectural correction

- [ ] 10.1 **Session `typescript-migration-contracts`; owns only `tests/repository/typescript-migration.test.ts` and `tests/repository/typescript-migration-inventory.json`:** Before any rename, record and independently verify the baseline 134 `.js`/`.mjs`/`.cjs` files as 128 wave-owned migration targets, three intentional authored `.mjs` paths, generated Unicode data, vendored pako, and the immutable noop fixture. The transition-safe test requires exactly one baseline `.mjs` or successor `.ts` path for every authored target, never both or neither; it verifies the fixed exclusions and ownership partition but does not assert that all 128 conversions are already complete. Run `node --test tests/repository/typescript-migration.test.ts` and `pnpm test:repository` GREEN before 10.2; do not edit protocol fixtures or expected results. **Depends on 9.3; blocks every conversion wave.**
- [ ] 10.2 **Session `typescript-migration-foundation`; owns root TypeScript/Biome/package/Turbo configuration, `.github/workflows/ci.yml`, `scripts/**` except `scripts/release/**`, and `tests/repository/**` except the two 10.1 files:** Add and run focused RED contracts only for this root slice, then explicitly enable `strict`, `noImplicitAny`, `useUnknownInCatchVariables`, `exactOptionalPropertyTypes`, `noUncheckedIndexedAccess`, and `forceConsistentCasingInFileNames`, forbid explicit and implicit `any`, and convert owned scripts/tests to Node.js 24-native erasable TypeScript. Extract typed argument-array launch helpers using `process.execPath`, resolved entrypoints, and `shell: false`. For paths renamed by 10.3–10.6, use one centralized bounded table of exact baseline/successor pairs that accepts exactly one existing path and fails on ambiguity or absence; no glob fallback or silent skip is allowed. Keep CI jobs exactly `check`, five `test (...)` groups (`core`, `typescript`, `python`, `protocol`, `examples`), `package`, and focused `windows`. Make the focused contracts, `pnpm test:repository`, `pnpm typecheck`, and `pnpm ci:verify-projects` GREEN before 10.3 or 10.4 starts. **Depends on 10.1.**
- [ ] 10.3 **Session `core-cli-typescript`; owns `packages/core/**` and `packages/cli/**` only:** Add and run RED final-state contracts for this package slice, then convert its authored production, test, worker, and pack files to TypeScript. Compile core and CLI production sources to deterministic runtime JavaScript plus declarations; point package-owned exports, bundled-core staging, and the CLI `remote-skills` bin at emitted output; preserve the CLI shebang as the first bytes and executable tar mode. Keep vendored `packages/core/src/build/vendor/pako-deflate.mjs` and its license separate and unchanged. Make those contracts, package-local checks, two identical local packs, clean offline direct/no-install CLI execution, and `pnpm test:repository` GREEN before a dependent wave starts. **Depends on 10.2.**
- [ ] 10.4 **Session `client-typescript`; owns `packages/sdk-typescript/**` only:** Add and run RED final-state worker, source-inventory, package-surface, declaration, and process contracts, then convert every authored JavaScript file except `src/cache/protocol-worker.mjs`. Move that shim's argument parsing, IO, orchestration, cache/protocol decisions, and error handling into an adjacent typed implementation so the stable `.mjs` path only imports and launches it. Compile production TypeScript to deterministic runtime JavaScript plus declarations and preserve ESM import, browser rejection, and CJS require rejection. Make the owned contracts, package-local protocol/cache/session tests, clean offline tarball session, and `pnpm test:repository` GREEN before a dependent wave starts. **Depends on 10.2.**
- [ ] 10.5 **Session `protocol-typescript`; owns authored `tests/protocol/**` and `tests/helpers/**` only:** Add and run RED final-state adapter/native-runner and cross-process contracts, then convert protocol tests, helpers, tools, and Node workers to Node.js 24-native erasable TypeScript without changing fixture bytes or expected results. Keep immutable `tests/protocol/fixtures/adapters/typescript-noop.mjs` unchanged. Keep `tests/protocol/adapters/typescript-protocol-adapter.mjs` as a stable import/launch-only shim and move all other logic into an adjacent typed implementation. Make the owned contracts, `pnpm test:protocol`, determinism, TypeScript/Python adapter and cache-race slices, and `pnpm test:repository` GREEN before 10.7. **Depends on 10.3 and 10.4.**
- [ ] 10.6 **Session `remaining-typescript-surfaces`; owns authored JavaScript under `apps/docs/**`, `examples/**`, and TypeScript helpers under `packages/sdk-python/tests/**` only:** Add and run RED final-state native-execution and public-example contracts, then convert those files to Node.js 24-native erasable TypeScript. Leave declarative `apps/docs/postcss.config.mjs` unchanged; preserve Python/TypeScript worker interoperability, Windows-safe child processes, Git/static-host behavior, docs routes, and public examples. Make each owned workspace check, the `examples` CI group, and `pnpm test:repository` GREEN before 10.7. **Depends on 10.2, 10.3, and 10.4.**
- [ ] 10.7 **Session `typescript-path-convergence`; owns only root `package.json`, TypeScript/Biome/Turbo configuration, `.github/workflows/ci.yml`, `scripts/**` except `scripts/release/**`, and `tests/repository/**`:** After 10.3–10.6, add and run RED contracts that reject the still-present transition table and any stale baseline path reference. Update root command strings, CI group dispatch, package/readiness/no-publication inspection, schema/project gates, process launchers, and repository assertions to the exact final typed source and emitted runtime paths; remove the bounded transition table without changing the eight-lane topology. Make the focused contracts, `pnpm test:repository`, `pnpm typecheck`, and `pnpm ci:verify-projects` GREEN before handoff. Do not edit any release-automation path. **Depends on 10.5 and 10.6.**
- [ ] 10.8 **Release-automation handoff; owned only by the independent release-automation owner:** Migration owners never create, edit, delete, or absorb `.github/workflows/prepare-release.yml`, `.github/workflows/publish-release.yml`, `scripts/release/**`, or `docs/maintainers/releases.md`. The migration owner supplies final emitted-package, shared-tool, and command paths after 10.7. If its separate task requires reconciliation, only the independent owner creates or edits those paths and returns focused workflow/release-test evidence; if the paths are absent or already compatible, it returns explicit no-op evidence. **Depends on 10.7.**
- [ ] 10.9 **Session `typescript-migration-convergence`; read-only until a failure is isolated:** From a fresh clone run `pnpm install --frozen-lockfile`, `uv sync --locked --all-packages`, `pnpm test:repository`, `pnpm test:protocol`, `pnpm typecheck`, `pnpm check`, `pnpm ci:verify-projects`, `pnpm package:check`, `pnpm publication:readiness`, `node scripts/check-no-publication.ts`, and `openspec validate build-remote-skills-v0 --strict`; run `pnpm windows` on Windows and all eight exact CI lanes. Verify every persistent wave-owned final-state contract, no explicit/implicit `any`, no unexpected authored JavaScript, identical deterministic artifacts, clean offline installs, release-owner focused/no-op evidence, no registry access, and a clean tree. Repeat contract review and fixes until the migration, readiness, and no-publication gates converge to zero failures. **Depends on 10.7 and 10.8.**

## 11. Final v0 acceptance

- [ ] 11.1 **Session `v0-acceptance`; read-only until a failure is isolated:** From a fresh clone, run frozen pnpm/uv installs, format, lint, typecheck, package-local tests, all shared scope/version/cache fixtures, multi-process cache tests, deterministic builds, Git-backed/static hosting examples, docs production build and agent-readable routes, local package build/inspect/install checks, strict-TypeScript convergence, and strict OpenSpec validation through every primary Linux lane; separately run the focused Windows installed-product contract. Verify zero skipped/focused tests outside that Windows contract, no registry access, and a clean tree. **Depends on 7.2, 7.3, 7.4, 7.5, 8.3, 9.3, and 10.9.**
- [ ] 11.2 **Session `v0-acceptance`; owns final evidence report only:** Record exact tool versions, commands, platform matrix, local artifact hashes, test counts, known v0 limitations, strict-TypeScript inventory status, release-owner handoff evidence, and publication-readiness status while explicitly stating that no registry configuration, name reservation, tag, upload, or publication occurred; verify the report is reproducible and references the tested commit. **Depends on 11.1.**
- [ ] 11.3 **Maintainer v0 completion decision — no publication:** Review the acceptance evidence and confirm or decline that the local `0.0.1` artifacts and documentation satisfy v0. This decision SHALL NOT authorize or trigger registry configuration, package publication, tag creation, upload, or deployment. **Depends on 11.2.**
