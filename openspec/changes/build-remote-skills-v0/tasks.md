## 1. Approval gates

- [x] 1.1 **Approval owner — no code:** Review and explicitly approve the complete OpenSpec proposal, capability specs, design, and this dependency map; verify `openspec validate build-remote-skills-v0 --strict` passes. **Blocks every task below.**
- [x] 1.2 **README owner — no code:** Draft the complete minimal replacement README from the approved OpenSpec, paste it into the user conversation, revise it until explicitly approved, and only then replace `README.md`; verify the approved text and committed file are byte-equivalent. **Depends on 1.1 and blocks every implementation task.**

## 2. Repository foundation

- [ ] 2.1 **Session `repo-foundation`; owns root manifests/config only:** Create the pnpm workspace, Turborepo graph, Node.js 24 baseline, uv Python workspace, Apache-2.0 license, formatting/lint/typecheck/test root commands, and package/app/example directories from `design.md`; verify clean installs and empty-package checks on a fresh clone. **Depends on 1.2.**
- [ ] 2.2 **Session `repo-foundation`; owns CI bootstrap only:** Add Linux/macOS/Windows CI shells, dependency caching, deterministic environment variables, and root `check` orchestration without publication credentials; verify all three matrix jobs execute placeholder package gates rather than silently skipping projects. **Depends on 2.1.**
- [ ] 2.3 **Session `repo-foundation`; owns schema tooling and generated-schema checks:** Add the strict `remote-skills.json` JSON Schema, schema publication path, unknown-field rejection tests, and a drift check tying runtime validators to the checked-in schema; verify valid defaults and invalid typo fixtures in both direct CLI tests and schema validation. **Depends on 2.1.**

## 3. Shared protocol contract

- [ ] 3.1 **Session `protocol-contracts`; owns `tests/protocol/**` and shared error definitions:** Define stable error codes, normalized result shapes, catalog fixtures, request transcripts, redaction expectations, and static expected results; capture RED in both SDK test adapters before either implementation can pass. **Depends on 2.1 and 2.3.**
- [ ] 3.2 **Session `protocol-contracts`; owns archive/path/limit fixtures:** Add exact valid and adversarial `skill-md`, tar-gzip, ZIP, traversal, duplicate, case-collision, link, special-file, invalid-UTF-8, decompression, count, and size fixtures without generating expectations from code under test; verify fixture integrity with independent SHA-256 manifests. **Depends on 3.1.**
- [ ] 3.3 **Session `protocol-contracts`; owns cache/network fixtures:** Add versioned cache-layout, partial-writer, crashed-lease, cross-process, HTTP validator, redirect, DNS/IP policy, retry, offline, removal, and credential-forwarding fixtures; verify the fixture server records exact requests and never stores secret values in snapshots. **Depends on 3.1.**
- [ ] 3.4 **Session `protocol-contracts`; owns deterministic publisher goldens:** Add canonical source trees plus independently reviewed exact index, Markdown, tar-gzip, ZIP, normalized metadata, and digest expectations; verify fixture bytes are stable when unpacked and repacked by an independent inspection script. **Depends on 3.2.**

## 4. Publisher CLI

- [ ] 4.1 **Session `publisher-validation`; owns `packages/core/src/authoring/**` and CLI validate/config modules:** Write RED tests, then implement Agent Skills frontmatter validation, directory/name rules, strict declarative config, safe default exclusions, `.skillignore`, filesystem boundary checks, symlink/hard-link rejection, local-reference validation, warning/error separation, and `--strict`; verify package-local tests and relevant protocol fixtures pass. **Depends on 3.1 and 3.2.**
- [ ] 4.2 **Session `publisher-build`; owns `packages/core/src/build/**` and CLI build modules:** Write RED deterministic fixtures, then implement `skill-md` selection, forced archives, normalized tar-gzip/ZIP encoders, content-addressed filenames, relative v0.2.0 index generation, limit enforcement, and atomic output replacement; verify byte-exact goldens on the current OS and two consecutive builds. **Depends on 4.1 and 3.4.**
- [ ] 4.3 **Session `publisher-dev`; owns CLI dev/server modules:** Write RED local-origin and interrupted-rebuild tests, then implement loopback `127.0.0.1:8787` serving, host/port options, explicit `--unsafe-host`, source watching, atomic completed-output switching, graceful shutdown, and no push protocol; verify a real local HTTP consumer observes only complete catalog generations. **Depends on 4.2.**
- [ ] 4.4 **Session `publisher-verify`; owns CLI verify/network modules:** Write RED private-origin and tampering tests, then implement full-origin verification, `--header`, `--header-env`, credential-safe diagnostics, network/redirect policy, bounded timeout/retries, and every-entry failure aggregation; verify local authenticated origins, CDN header isolation, digest mismatch, timeout, and private-address redirect fixtures. **Depends on 4.2 and 3.3.**
- [ ] 4.5 **Session `publisher-cli-gate`; owns CLI package manifest/entrypoint/tests only:** Wire `@remote-skills/cli` with binary `remote-skills`, help/version output, exit codes, package contents, and clean `npx @remote-skills/cli` smoke tests; verify `validate`, `build`, `dev`, and `verify` invoke only their intended boundaries and that no `bridge`, `deploy`, or `publish` command exists. **Depends on 4.3 and 4.4.**

## 5. TypeScript consumer SDK

- [ ] 5.1 **Session `typescript-client`; owns `packages/sdk-typescript/src/catalog/**` and origin configuration:** Write RED catalog fixtures, then implement explicit aliases, compact aggregate and single-origin discovery, v0.2.0-only schema handling, unknown-field tolerance, standard HTTP caching, partial failure, strict aggregate mode, URL resolution, headers, timeout/retry, and network policy; verify exact request transcripts and stable error codes. **Depends on 3.1 and 3.3.**
- [ ] 5.2 **Session `typescript-cache`; owns `packages/sdk-typescript/src/cache/**`:** Write RED atomicity and lease tests, then implement OS-default `cache-v1`, memory/custom caches, verified no-clobber publication, catalog metadata, cross-process leases, crash cleanup, bounded LRU eviction, and credential exclusion; verify multi-process readers never observe partial state. **Depends on 3.3.**
- [ ] 5.3 **Session `typescript-activation`; owns `packages/sdk-typescript/src/activation/**` and activated resource types:** Write RED artifact fixtures, then implement digest-first activation, `skill-md`, bounded tar-gzip/ZIP extraction, normalized safe paths, standard frontmatter parsing, immutable session pins, `list`, UTF-8 `read`, `readBytes`, and no execution; verify all archive/path/limit fixtures. **Depends on 5.1, 5.2, and 3.2.**
- [ ] 5.4 **Session `typescript-session`; owns TypeScript client/session facade and lifecycle tests:** Write RED update/offline/removal tests, then expose `createRemoteSkills`, aggregate `catalog`, origin-bound `session`, `activate`, future-session `refresh`, stale-age metadata, removal behavior, `close`, and async disposal; verify D1 remains pinned while D2/removal affects only later sessions. **Depends on 5.3.**
- [ ] 5.5 **Session `typescript-package-gate`; owns TypeScript manifest/exports/browser rejection/package smoke tests:** Publish a server-runtime-only `@remote-skills/client` surface with declarations, ESM/CJS strategy chosen consistently, no browser bundle, and clean-install examples; verify packed installation and import on Node.js 24 with no workspace resolution. **Depends on 5.4.**

## 6. Python consumer SDK

- [ ] 6.1 **Session `python-client`; owns `packages/sdk-python/src/remote_skills/catalog*` and origin configuration:** Write RED shared catalog fixtures, then implement the async explicit-origin catalog/network behavior with exact TypeScript-equivalent requests, v0.2.0 handling, caching validators, partial failures, headers, timeout/retry, and network policy; verify stable error-code parity. **Depends on 3.1 and 3.3.**
- [ ] 6.2 **Session `python-cache`; owns Python cache modules:** Write RED shared-layout and process-race tests, then implement OS-default `cache-v1`, memory/custom caches, atomic immutable publication, leases, crash cleanup, bounded LRU, and credential exclusion; verify Python reuses TypeScript-written fixtures and vice versa. **Depends on 3.3.**
- [ ] 6.3 **Session `python-activation`; owns Python activation/archive/resource modules:** Write RED shared artifact fixtures, then implement digest-first activation, `skill-md`, safe bounded tar-gzip/ZIP extraction, standard parsing, immutable pins, async `list`/`read`/`read_bytes`, and no execution; verify exact normalized parity across every archive/path/limit fixture. **Depends on 6.1, 6.2, and 3.2.**
- [ ] 6.4 **Session `python-session`; owns Python facade/lifecycle tests:** Write RED update/offline/removal tests, then expose `RemoteSkills`, aggregate catalog, async origin session/context manager, activation, future-session refresh, stale metadata, removal behavior, and explicit async close; verify behavior matches TypeScript golden results. **Depends on 6.3.**
- [ ] 6.5 **Session `python-package-gate`; owns `pyproject.toml`, Python exports, build/smoke tests:** Produce the `remote-skills` wheel and sdist for Python 3.11+, import as `remote_skills`, exclude workspace-only sources, and verify clean uv installation and async quickstart execution. **Depends on 6.4.**

## 7. Cross-language and security convergence

- [ ] 7.1 **Session `protocol-parity`; owns only shared fixture adapters/expected-result corrections:** Run every protocol fixture through publisher, TypeScript, and Python; resolve semantic differences without weakening fixtures and verify exact outputs, stable errors, request counts, and cache effects are identical. **Depends on 4.5, 5.5, and 6.5.**
- [ ] 7.2 **Session `cache-race-gate`; owns cross-process integration tests:** Exercise simultaneous TypeScript/Python downloads, losing writers, partial crashes, lease expiry, active-pin eviction, cache corruption, and stale temporary cleanup against one real directory; verify no partial/corrupt observation and no active artifact eviction. **Depends on 7.1.**
- [ ] 7.3 **Session `network-security-gate`; owns adversarial integration tests:** Exercise same-host and cross-host headers, redirect chains, DNS/IP changes, loopback development exceptions, userinfo/query sanitization, request limits, retry exhaustion, and secret canaries; verify secrets appear only on their configured host and never in cache, errors, debug events, or snapshots. **Depends on 4.4, 5.5, and 6.5.**
- [ ] 7.4 **Session `determinism-gate`; owns cross-platform golden checks:** Run publisher goldens on Linux, macOS, and Windows and compare raw artifact/index hashes, then consume each platform's output in both SDKs; verify byte identity and interoperability with cache disabled and enabled. **Depends on 7.1 and CI task 2.2.**

## 8. Documentation and examples

- [ ] 8.1 **Session `docs-site`; owns `apps/docs/**` only:** Build the self-hostable Fumadocs site with conceptual overview, publisher/hosting, CLI, TypeScript, Python, cache/update/offline, authentication, trust/security, configuration, API reference, and release pages; verify links, navigation, production build, and boundary-language tests. **Depends on approved README task 1.2 and stable APIs 4.5, 5.5, 6.5.**
- [ ] 8.2 **Session `examples`; owns `examples/**` only:** Implement runnable publisher, TypeScript consumer, Python consumer, and generic static-host examples using only public package surfaces; verify publisher output through `verify` and run both consumers against the same local origin. **Depends on 4.5, 5.5, and 6.5.**
- [ ] 8.3 **Session `docs-contract`; owns snippet extraction/tests and may edit docs examples but not APIs:** Compile or execute every README/docs/config snippet, ensure the minimal README does not claim adapters or hosting, and verify documentation accurately distinguishes byte integrity from content trust and context-lazy reads from archive download behavior. **Depends on 8.1 and 8.2.**

## 9. Packaging and trusted releases

- [ ] 9.1 **Session `package-release`; owns package manifests and release dry-run scripts:** Add coordinated version metadata starting at `0.0.1`, independent package boundaries, Apache-2.0 package contents, npm pack and uv build checks, SBOM/provenance inputs, and clean installation smoke tests; verify no workspace-only dependency or unpublished file leaks into artifacts. **Depends on 4.5, 5.5, and 6.5.**
- [ ] 9.2 **Session `trusted-publishing`; owns `.github/workflows/release*` and release docs:** Add protected tag-driven npm and PyPI Trusted Publishing with OIDC, attestations/provenance where supported, explicit environment approval, all-platform prerequisites, and no long-lived registry secrets; verify workflow static policy tests and registry dry runs without publishing. **Depends on 2.2 and 9.1.**
- [ ] 9.3 **Maintainer action `reserve-pypi` — external, not delegated:** Configure the pending PyPI Trusted Publisher for project `remote-skills`, owner `ranst91`, repository `remote-skills`, the approved release workflow, and protected `pypi` environment; publish the legitimate `0.0.1.dev0` package only after all required approval and package gates, then verify ownership and trusted publisher conversion on PyPI. **Depends on 9.2 and explicit user authorization at execution time.**

## 10. Final v0 acceptance

- [ ] 10.1 **Session `v0-acceptance`; read-only until a failure is isolated:** From a fresh clone, run frozen pnpm/uv installs, format, lint, typecheck, package-local tests, all shared fixtures, multi-process cache tests, deterministic builds, examples, docs production build, package dry runs, and strict OpenSpec validation on every supported matrix; verify zero skipped/focused tests and a clean tree. **Depends on 7.2, 7.3, 7.4, 8.3, and 9.2.**
- [ ] 10.2 **Session `v0-acceptance`; owns final evidence report only:** Record exact tool versions, commands, platform matrix, artifact/package hashes, test counts, known v0 limitations, and publication readiness without claiming PyPI publication unless 9.3 actually occurred; verify the report is reproducible and references the tested commit. **Depends on 10.1.**
- [ ] 10.3 **Maintainer release decision — external:** Review the acceptance evidence, ensure the approved README and docs match shipped behavior, and explicitly authorize or decline the `0.0.1` release; no automated task may publish the non-development release without this decision. **Depends on 10.2 and, if name reservation is required first, 9.3.**
