[project]
name = "remote-skills-workspace"
version = "0.0.0"
requires-python = ">=3.11"
dependencies = []

[tool.uv]
package = false

[tool.uv.workspace]
members = ["packages/sdk-python", "integrations/langchain-python", "examples/langchain"]
